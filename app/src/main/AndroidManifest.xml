<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.neuralgorithmic.rentathon">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Product.GenerateQRCode"></activity>
        <activity android:name=".Rent.RentalDetails" />
        <activity android:name=".Rent.QRScan" />
        <activity android:name=".Product.BackgroundCheck" />



        <!-- FIREBASE CLOUD MESSAGING HERE -->
        <service
            android:name=".CloudMessaging.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/rentathon_logo_cart" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />



        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".Signin.SignUpProfilePic" />
        <activity android:name=".MessagingHome" />
        <activity android:name=".Product.ProductOverLord" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCHBd7NQPWdOUor_B18mGpbrvBSkHo8gZc" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <activity android:name=".Product.PreviewProduct" />
        <activity android:name=".Rent.ProductInsights" />
        <activity android:name=".Rent.EditProduct" />
        <activity android:name=".Rent.MorePhotos" />
        <activity android:name=".Rent.Payment" />
        <activity android:name=".Profile.VerifyEmail" />
        <activity android:name=".Misc.WelcomeProdcutMain" />
        <activity android:name=".Profile.ConfiimEdit" />
        <activity android:name=".Profile.EditProfile" />
        <activity android:name=".Profile.UserHomeMain" />
        <activity android:name=".Profile.UserStatsMain" />
        <activity android:name=".Rent.RentProductTime" />
        <activity
            android:name=".Rent.RentProductMain"
            android:theme="@style/AppTheme.SlidrActivityTheme" />
        <activity android:name=".Profile.ProfileMain" />
        <activity android:name=".Product.ProductMain" />
        <activity android:name=".Signin.SignUpPage2" />
        <activity android:name=".Home.Home" />
        <activity android:name=".Signin.SignUpPage1" />
        <activity android:name=".Signin.MainActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/PictureCropperTheme" />
        <activity android:name=".Misc.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>